#include <common.h>

class Solution
{
public:
    int countSquares(vector<vector<int>> &matrix)
    {
        vector<vector<int>> sums;
        for (int i = 0; i < matrix.size(); i++)
        {
        }
    }
};